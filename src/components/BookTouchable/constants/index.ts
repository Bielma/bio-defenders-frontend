import { StyleSheet } from "react-native";
import theme from "../../../theme";
export const BookTouchableStyles = StyleSheet.create({
  container: {
    backgroundColor: theme.colors.primaryTwo,
    height: 60,
    width: 60,
    borderRadius: 30,
    alignItems: "center",
    justifyContent: "center",
  },
});
